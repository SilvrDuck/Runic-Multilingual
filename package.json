{
  "name": "tunic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "npm test",
    "build": "node esbuild.js --production",
    "dev": "node esbuild.js --watch",
    "test": "esbuild scripts/verify_examples.ts --bundle --platform=node --format=esm --external:espeak-ng --outfile=.verify-tmp.mjs --log-level=warning && node .verify-tmp.mjs",
    "format": "prettier -w src/**/*.{ts,tsx}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.7.4",
    "ejs": "^3.1.10",
    "esbuild": "^0.23.1",
    "esbuild-plugin-copy": "^2.1.1",
    "preact": "^10.23.2",
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "espeak-ng": "^1.0.2"
  }
}
